@page "/products/{id}"
@inject IHttpClientFactory _httpClient;
@inject ICartService _addToCartService



@if (Product is null)
{
    <PageTitle>No Product</PageTitle>
    <h1 class="text-center mt-3 text-light">No Product</h1>
}
else
{
    <PageTitle>@Product.Name</PageTitle>
    <h1 class="text-center mt-3 text-light">@Product.Name</h1>
    <div class="container mt-5 mb-5">
        <div class="card card-custom">
            <div class="row g-0 d-flex justify-content-center p-3">
                <div class="col-md-6">
                    <div class="d-flex flex-column justify-content-center">
                        <div class="d-flex justify-content-center">
                            <img src="@Product.Image" alt="@Product.Name Image" >
                        </div>
                    </div>
                </div>
            </div>
            <div class="row g-0 d-flex justify-content-center">
                <div class="col-md-6 w-50 text-center">
                    <div class="p-3">
                        <div class="">
                            <h3>@Product.Name</h3>
                        </div>
                        <div class="d-flex flex-row align-items-center justify-content-center">
                            <span>@Product.Category.Replace(" ", ", ")</span>
                        </div>
                        <div class="mt-2 content">
                            <p>@Product.Description</p>
                        </div>
                        <h3>@Product.Price kr</h3>
                        <div class="buttons d-flex justify-content-center flex-row mt-5 gap-3">
                            <button class="btn btn-outline-light text-light" @onclick="() => _addToCartService.BuyOnClickAsync(Product)">Buy Now</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

